module.exports = {
    MODEL_NAME: "#{ModelName}#",
    MODEL_PROPERTIES_SECTION: "#{ModelPropertiesSection}#",
    ROUTES_SECTION: "#{RoutesSection}#",
    DB_CONFİG: {
        NAME: "#{DbName}#",
        USER: "#{DbUser}#",
        PASSWORD: "#{DbPassword}#",
        SERVER: "#{DbServer}#",
        PORT: "#{DbPort}#",
        DIALECT: "#{DbDialect}#", // mssql, mysql, postgres, mariadb, sqlite
        MODEL_LIST: "#{ModelList}#"
    }
}